import React from 'react';
import { Button } from '@kubed/components';

const Login = () => {
  return (
    <div>
      <Button shadow={true} color={'secondary'} radius={'lg'}>
        Button
      </Button>
    </div>
  );
};

export default Login;
